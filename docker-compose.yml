services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    # volumes:
    #   - ./backend:/app
    #   - /app/node_modules
